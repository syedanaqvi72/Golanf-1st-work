package main

import "fmt"

var val2 int = 100

func tant() {
	//\n creates new lines in output if i write thi sin fmt.Print (val2, /n)
	//local: variables which are declared inside the function //
	//Global: variable declared outside the fuction //
	//Package-level//
	var val = 55
	fmt.Println(val)
}
